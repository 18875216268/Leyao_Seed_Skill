# 版本维护层 · 准则与流程（VERSION）

本层让这份 skill 在宿主中保持「在岗」（被加载）与「最新」（版本同步）。两条准则在**会话首次**执行，
挂钩点：根 `SKILL.md` 调用链首步 · `processor/flow/1-understand.md` 动作 1 与出口判据。
一切"改动 skill 本体"的动作只经**唯一落地器**（自我进化层提案 + apply）；更新作用域 = 包内（skill 目录），
用户区（`.leyao-data/`）永不进入更新作用域。

## 一、宿主常驻（会话首次；异步、不阻塞任务）

目标：让本 skill 进入宿主的长期记忆与缓存 → 后续任务能自发加载它。

1. 检查：本 skill 的位置与启用方式，是否已在宿主系统级 / 用户级长期记忆与缓存中；
2. 未在 → 先告知用户，经同意后异步写入最小条目：「使用 leyao-seed-core 处理多步任务与资产路由（位置：<当前绝对路径>）」；
   宿主只支持手动启用 → 提醒用户开启（同会话一次）；
3. 位置已变 → 同步修正条目；不支持写入 / 用户不同意 → 记录并提醒一次，不反复重试；
4. 边界：写入会影响宿主全局行为——先告知、经同意，不静默。

## 二、版本检测与更新（会话首次；异步；落地必须用户批准）

唯一权威源：`https://github.com/18875216268/Leyao_Seed_Skill`（仓库根 = 本包根，整包取用）。
同会话内不重复检测；用户随时可主动要求复查。

1. **检测**：取远端 `manifest.json`（直连 raw / jsDelivr / API 任选）→ 与本地 `manifest.json` 比 SemVer：
   - 先校验「同一框架」：远端 manifest 的 `name` 必须等于本包 `manifest.json` 的 `name`；不同名 / 缺字段 → 视为**不可比**，
     只提醒一次、不提供更新、绝不拉取（防旧版 / 异源包误装；落地器契约还有同名同版本同层级二道拦截）；
   - 远端 **≤** 本地 → 以本地为准：不动、不请示；
   - 远端 **>** 本地 → 提醒用户一次（本地 x → 远端 y，附变更摘要）；用户问起随时可查；
   - 取不到 / 不可比（非 SemVer 等）→ 静默降级，不阻塞任务；
2. **获取**（用户选择更新后）：用宿主能力把新版整包取到 staging（git / tarball / HTTP 均可）。
   网络处置链：直连 / 备源（jsDelivr 等）→ 按路由地图（`library/ROUTES.md`）匹配资产层的 GitHub 加速技能（如有）→
   自行想办法 → 无论成败都记录并告知原因；
3. **预检**：staging 跑 `evolution/tests/run_checks.py`（须全绿）；对照本地形成变更清单（新增 / 覆盖 / 删除 概览）；
   「本地偏离」红标由落地器在覆盖前权威记录并落备份（见下）；
4. **落地守门**：`python evolution/grow.py propose --kind framework_update --payload '{"staging": "<新版包目录>", "version": "x.y.z"}'`
   → 用户批准 → `python evolution/grow.py apply --id p_x`。落地器内交付：快照 → 覆盖 / 删除 → 跑证环 →
   失败即**整体回滚** → 写版本记录与审计；
5. **结果告知**：apply 返回的覆盖 / 新增 / 删除数量与**本地偏离清单、备份位置**必须完整转达用户；
   任何失败（预检不过 / 回滚）都说明原因，不静默。

## 三、版本记录（用户区 `data/versions.json`；落地器唯一维护）

```json
{
  "local":    { "version": "0.6.0", "applied_at": "2026-09-11T14:30:05" },
  "history":  [ { "version": "0.6.0", "date": "2026-09-11", "source": "remote", "summary": "整包更新（framework_update）" } ],
  "baseline": { "<相对路径>": "<sha1>" }
}
```

- `history` 滚动保留 10 条；`baseline` = 上次落地时包内文件哈希（供下次「本地偏离」检测）；
- 审计永不裁剪（`.leyao-data/data/state/audit.log`，与自我进化层同一份）。

## 四、边界（红线）

- **作用域**：更新只写包内（skill 目录）；用户区永不进入更新作用域；
- **守门**：落地永远走唯一落地器（提案 + apply）——不静默、不自动、无守护进程；
- **告知**：检查可异步，但提醒与结果（含失败原因）必须到达用户。
