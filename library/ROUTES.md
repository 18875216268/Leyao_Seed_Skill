# 资产管理层 · 总路由地图
> ★ 默认资产（每次任务必读）：`pp32an` **@行业知识库** → `library/assets/pp32an/`——卡在**用户数据区** `data/assets/pp32an/card.md`（框架挂载态 `.leyao-data/…`；独立态 `~/.leyao-kb/card.md`）；只用于识别与定位（定义以池 authority 为准）；读法与刷新见其 `references/card.md`，判据见 `processor/flow/3-execute.md` 0.5。
> 读者：agent 与审阅者；**维护**请用管理台（`library/admin/`）或 `routes.json`（唯一事实源，本图由 `engine.py` 生成）。
> 路由：按节点**描述**匹配 → 命中进其 `→ 挂载` 目录读 `SKILL.md`／`README.md` 调用；无命中按自带判据亲做。描述形态：六段齐备=判据链全能力；`（自由描述·降级匹配）`=关键词级；`（无描述·不可路由）`（模板见 `processor/shapes.md` 第 7 节）。
> 级联：`（N 个子节点 → 局部图 library/routes/<id>.md）` → 读局部图继续匹配（可再分片 → 任意级联），叶节点执行（分片阈值：子节点 > 5 或 子树节点 > 20）。
> 回写：交付后 `--routed <命中节点id>`；无命中写 `none`。同读用户区 `.leyao-data/data/memory.md`（命中「失效模式」先规避、命中「有效做法」直接复用）。

- `pp32an` **@行业知识库** `Skill包` → `library/assets/pp32an/`
  - _【何时用】需要确认业务术语/指标口径/制度规定/课程资料时（缺货率是什么、成本优势率怎么算、有没有培训课程）｜【不适用】需要业务数据（→ p3nes3 / i7c4z1）；制作 PPT/流程图（→ bvix9o）；GitHub 连通（→ fmcq5n）｜【别名】知识库、术语查询、口径校验、云智库、乐药知识｜【输入前置】公共池免登录；云智库检索需其登录态（未登录明确提示人工扫码）｜【时效性】术语/口径随池更新（缓存 TTL 桶级（口径/制度/术语/课程 24h、搜索 1h））；制度/课程即时查询｜【回退】两库未命中 → 如实拒答 + 建议（换说法 / --expand / 请维护者补池），不编造。_
- `bvix9o` **@通用导航库** `Skill包` → `library/assets/bvix9o/`
  - _【何时用】需要找各种方法论、需要制作 PPT、绘制流程图、把抽象观点变成图、降低文本 AI 味，或「再找一份可复用技能」时（万能套件）｜【不适用】需要业务数据（→ p3nes3 / i7c4z1）；需要 GitHub 连通（→ fmcq5n）；普通问答｜【别名】万能套件、公共技能、技能合集｜【输入前置】你手上的原始素材（文档/大纲/数据）；无需凭证｜【时效性】随包更新，无实时数据依赖｜【回退】套件内无对应技能 → 按自带判据亲做，交付后提案补资产！_
- `fmcq5n` **github-web-skill** `Skill包` → `library/assets/fmcq5n/`
  - _【何时用】本机网络正常但访问 GitHub 不可达时（clone/pull/push 失败、raw 或 Release 下载不动、github.com 打不开、DNS 污染、代理变量干扰）｜【不适用】非 GitHub 站点；内网系统访问问题；需要业务数据（→ p3nes3 / i7c4z1）｜【别名】GitHub 连通、github 加速、gh 访问层｜【输入前置】目标仓库/文件 URL；系统 git 与 curl；hosts 动作需管理员权限｜【时效性】镜像/线路实时漂移，工具自带并发探活与冷却重试｜【回退】全部通道失败 → 如实告知 + 离线指引（写清试过哪些、为什么失败）_
- `p3nes3` **Pms-乐药查询** `Skill包` → `library/assets/p3nes3/`（1 个子节点）
  - _【何时用】需要 PMS（乐药）业务数据且**要求实时性较强**：出库/促销/基础数据等按内置板块参数筛选查询（当天数据，含税销售/P4 毛利等）｜【不适用】需要自定义聚合/计算字段（→ i7c4z1）；需跨板块自由 SQL；非 PMS 数据源；只要 T+1 数据（→ i7c4z1 更合适）｜【别名】Pms、乐药查询、PMS 取数、促销查询｜【输入前置】PMS 凭证（登录获取或用户提供）；板块与参数口径｜【时效性】实时性较强（促销毛利 > 其它板块）｜【回退】促销毛利（尤其当天实时）→ **直接走子节点 psol5x（首选，非补救）**；其余板块取不到 → 退回任务层亲做。并选提示：既要当天实时又要自定义聚合 → 与 i7c4z1 同时成立 → 按门槛问用户；其余板块需先 `pms_sync` 拉取基础包（出厂为空）_
  - `psol5x` **Pms-促销毛利板块** `Skill包` → `library/assets/p3nes3/vendor/optimizers/Pms_促销毛利v1.08/psol5x/`
    - _【何时用】PMS 促销毛利的定向查询：**当天实时**（当天汇总/品种明细/订单明细，精确到时分秒）**及历史**（长周期按天拆分，单次 ≤48 小时）｜【不适用】应收边际利润（查不到）；需要自定义聚合/计算字段（→ i7c4z1）；非 PMS 促销范畴｜【别名】促销毛利、促销毛利板块、当天数据查询、促销毛利历史｜【输入前置】PMS 凭证；板块参数（时间范围口径，未指定默认当天）｜【时效性】当天实时；历史可查（按天拆分、单次 ≤48 小时，导出同样受限）｜【回退】无法解决 → 回退父级 p3nes3 的其它板块_
- `i7c4z1` **观远BI-乐药查询** `Skill包` → `library/assets/i7c4z1/`（1 个子节点）
  - _【何时用】需要业务数据且要**自定义筛选/聚合/计算**（出库统计、维度指标自由组合）；可接受数据**截止到前一天**（T+1）｜【不适用】要求当天实时数据（→ p3nes3 / psol5x）；非乐药 BI 域；只需内置板块固定参数（→ p3nes3 更直接）｜【别名】观远、BI 查询、自助取数、乐药 BI｜【输入前置】观远 BI 凭证（登录器 Cookie，或 PAT——**PAT 仅用于 bi-pat 通道**；Cookie 卡片/优化板需 Cookie 凭证）；数据集与字段口径｜【时效性】截止前一天（滞后）｜【回退】子节点 h4dsa6（出库统计 Ultra）；仍不行 → 退回任务层亲做。并选提示：既要实时又要聚合 → 与 p3nes3 同时成立 → 按门槛问用户_
  - `h4dsa6` **BI-自助取数Ultra板块** `Skill包` → `library/assets/i7c4z1/vendor/optimizers/BI-出库统计Ultra查询v1.08/h4dsa6/`
    - _【何时用】出库统计类取数：自定义筛选、聚合与计算字段/指标，筛选支持批量传值（万能取数端口）｜【不适用】当天实时数据（→ psol5x / p3nes3）；非出库主题的取数｜【别名】出库统计 Ultra、万能取数、自助取数 Ultra｜【输入前置】BI 凭证（随父级 i7c4z1）；筛选条件与指标定义｜【时效性】随父级：截止前一天｜【回退】无法解决 → 回退父级 i7c4z1_