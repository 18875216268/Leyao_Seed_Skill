# ⑤ 交付

按报告框架交付；口径随行；经验沉淀。

## 输入

验收步通过的产物与口径标注（含验收留证）。

## 出口判据（全过才算任务完成）

- [ ] 交付物位置与命名符合工作区约定
- [ ] 报告框架五要素齐备，口径随行
- [ ] 轨迹已追加（`grow.py trace`，一次任务一条）

## 动作

1. **按框架组织**：结论先行 → 关键发现（按重要性）→ 支撑细节（表格优先）→ 口径与假设（时间范围/主体/排除项）→ 后续建议（只给有依据的）。骨架见 `processor/shapes.md`。
2. **交付物落位（硬）**：沿用 ② 规划已确定并明示的 `<work-dir>`（中途变更须在交付说明开头明示新目录）。`<work-dir>/deliverables/` 只放最终确认版本；中间产物在 `work/`、原始输入在 `inputs/`、旧版本归档到 `archive/`（不静默删除）。
3. **口径随行**：每个关键数字带口径（时间范围/含税与否/主体范围）——不带口径的数字无法被复核、等于不可用，故**不出口**；事实给来源、推断给链条（混写会让读者分不清哪句可采信）。
4. **沉淀**：追加一条轨迹进自我进化层（**一次任务只追加一条**，多轮追问不重复追加）——`python evolution/grow.py trace --task "<任务>" --routed "<命中的节点 id / 无命中写 none>" --outcome success|partial|fail [--reason "<失败原因>"] [--override "<用户纠正>"]`。`--routed` **必须写节点 id**（见 `library/ROUTES.md` 各节点行首），它是规则归属与命中率统计的唯一依据；候选规则由 `grow.py reflect` / `grow.py evolve` 蒸馏落地（memory 自动档；文档 / 路由 / 资产内容走 `grow.py propose` 提案守门）。

## 红旗

- 交付物里混入中间产物。
- 经验直接手改进共享文档（绕过自我进化层提案）。

## 引导（可裁量，非判据）

- **"关键发现 3-5 条"是引导，不是判据**：确有 6 条以上且都重要时，附一节"次要发现"，不要硬压成 5 条，也不要全塞进主节。
- **结论先行的写法**：第一句要能独立成立（把结论放最前），别用"经过分析…"开头。
- **后续建议的门槛**：只给"有依据 + 可执行"的（依据 = 本次数据 / 资产文档 / 经验记忆）；"可以考虑"类空话不写。
- **交付前最后一问**：把交付物当成"别人只读它、不问你"来读一遍——读不出结论、或读不出数字口径的，就是还没写完。
