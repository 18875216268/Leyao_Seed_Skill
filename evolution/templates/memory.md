# 经验沉淀（L0 · 自我进化层自动维护）

> **为什么在用户区**：它是**执行期要给 AI 读的记忆**，与 `ROUTES.md` 并列——一个说"有哪些资产"，一个说"用它们学到了什么"；机器状态（棘轮/轨迹/规则表）在用户区 `.leyao-data/data/state/`，不进 prompt、不进交付。
> **何时读**：读 `library/ROUTES.md` 时**同读本文件**——命中的「失效模式」先规避或核验替代路径，命中的「有效做法」可直接复用。
> **四段 ↔ 状态**：`待验证`=active(route)、`失效模式`=active(avoid)、`有效做法`=core、`墓碑`=retired（指纹 + 否定理由，防糟粕复活）。长期稳定的精华由提案提炼进 `library/assets/` 内的相应文档后从本文件移除。
> **本文件为模板**：用户区首次初始化时按此播种；此后只由自我进化层自动维护。

## 失效模式

（无）

## 有效做法

（无）

## 待验证

（无）

## 墓碑

（无）
